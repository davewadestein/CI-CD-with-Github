name: Release Info
description: Generate a simple release identifier from GitHub context

inputs:
  environment:
    description: Deployment environment
    required: true
    default: staging

outputs:
  release-id:
    description: Generated release identifier
  short-sha:
    description: Short Git commit SHA

runs:
  using: node24
  main: index.js
